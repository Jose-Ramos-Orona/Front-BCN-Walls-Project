import styled from "styled-components";
import image from "./brick-wall.webp";

const CreateGraffitiPageStyled = styled.main`
  background-image: url(${image});
  background-repeat: no-repeat;
  background-size: cover;
`;
export default CreateGraffitiPageStyled;
